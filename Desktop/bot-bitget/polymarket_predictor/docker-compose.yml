version: "3.9"

services:
  monitor:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "8080:8080"    # dashboard web
    volumes:
      - ./data:/data
    command: >
      python telegram_bot.py
      --monitor
      --bankroll ${BANKROLL:-1000}
      --min-edge ${MIN_EDGE:-0.04}
      --topics ${TOPICS:-elon,geopolitica,deportes,crypto}
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
